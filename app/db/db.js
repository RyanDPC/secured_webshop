// Remplace par tes propres informations de connexion

// Importer les modèles Sequelize
const User = require("../models/User"); // Assure-toi que le modèle User est correctement importé

// Exporter à la fois `sequelize` et `initDb` pour les utiliser dans d'autres fichiers
module.exports = { initDb };
